package rasmcore:image@0.1.0;

/// Image filters and effects — AUTO-GENERATED by build.rs
///
/// Do not edit this file directly. To add a filter:
///   1. Add #[register_filter] to your domain function in filters.rs
///   2. Rebuild — build.rs regenerates this WIT, the adapter, and the manifest
interface filters {
    use rasmcore:core/types.{buffer, image-info};
    use rasmcore:core/errors.{rasmcore-error};

    // ─── Generated Filter Functions ───────────────────────────────────────
    //
    // Each filter below is auto-generated from #[register_filter] annotations.

{{GENERATED_FILTERS}}

    // ─── Static Declarations ─────────────────────────────────────────────

    /// Alpha composite foreground over background (Porter-Duff "over").
    composite: func(fg-pixels: buffer, fg-info: image-info, bg-pixels: buffer, bg-info: image-info, x: s32, y: s32) -> result<buffer, rasmcore-error>;

    /// Blend modes for compositing
    enum blend-mode {
        multiply,
        screen,
        overlay,
        darken,
        lighten,
        soft-light,
        hard-light,
        difference,
        exclusion,
        color-dodge,
        color-burn,
        vivid-light,
        linear-dodge,
        linear-burn,
        linear-light,
        pin-light,
        hard-mix,
        subtract,
        divide,
    }

    /// Blend two same-size images using the specified blend mode
    blend: func(fg: buffer, fg-info: image-info, bg: buffer, bg-info: image-info, mode: blend-mode) -> result<buffer, rasmcore-error>;
}
