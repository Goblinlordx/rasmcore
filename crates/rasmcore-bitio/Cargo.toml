[package]
name = "rasmcore-bitio"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Bit-level reader/writer for codec bitstreams — MSB and LSB ordering"

[dependencies]
# No external dependencies — pure Rust, WASM-ready
