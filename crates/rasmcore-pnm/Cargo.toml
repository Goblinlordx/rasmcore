[package]
name = "rasmcore-pnm"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Pure Rust PNM (PBM/PGM/PPM/PAM) encoder/decoder — zero dependencies"

[dependencies]
rasmcore-bitio = { path = "../rasmcore-bitio" }

[dev-dependencies]
