# Image Filter Reference Validation

All filters in `rasmcore-image` are validated against reference implementations
using a canonical 7-image test suite. This document records what matches,
what differs, and why.

## Quick Start

```bash
# Install all reference tools + Python venv + generate fixtures:
./scripts/setup-references.sh

# Run ALL reference/parity tests across all crates:
./scripts/run-reference-tests.sh
```

See `scripts/setup-references.sh --help` for options. Both scripts are idempotent.

## Reference Implementation

- **OpenCV 4.13.0** via `opencv-contrib-python-headless` (Python, in venv)
- Fixtures generated by `tests/fixtures/opencv/` generation script
- All fixtures are raw grayscale bytes (128×128, 8-bit)

## Canonical Test Image Set

| Image | Description | Purpose |
|-------|-------------|---------|
| `gradient_128` | Smooth horizontal gradient 0-254 | Low frequency, interpolation behavior |
| `checker_128` | 8px checkerboard (25/230) | High frequency, edge preservation |
| `noisy_flat_128` | Gaussian noise σ=20 around 128 | Denoising effectiveness |
| `sharp_edges_128` | Step function + rectangle (0/200/255) | Edge ringing, boundary handling |
| `photo_128` | Sinusoidal + noise | Realistic content |
| `flat_128` | All pixels = 128 | Degenerate / identity case |
| `highcontrast_128` | Sinusoidal extreme darks/brights | Full dynamic range |

## Filter Parity Results

### Bilateral Filter — Pixel-Exact

**Reference:** `cv2.bilateralFilter(img, d=9, sigmaColor=75, sigmaSpace=75)`

| Image | MAE | Max Error | Status |
|-------|-----|-----------|--------|
| gradient | 0.0000 | 0 | Exact |
| checker | 0.0000 | 0 | Exact |
| noisy_flat | 0.0000 | 0 | Exact |
| sharp_edges | 0.0000 | 0 | Exact |
| photo | 0.0000 | 0 | Exact |
| flat | 0.0000 | 0 | Exact |
| highcontrast | 0.0000 | 0 | Exact |

**Alignment details (required for pixel-exact match):**
- Circular kernel mask: pixels where `sqrt(dy² + dx²) > radius` are skipped (not square kernel)
- f32 accumulation: both spatial weight sum and value×weight sum use f32 (not f64)
- Color weight LUT: 256-entry table indexed by `|center - neighbor|` for grayscale
- RGB color distance: L1 norm `|b-b₀| + |g-g₀| + |r-r₀|` (not Euclidean)
- Spatial weight: `exp(-(dy² + dx²) × (-0.5 / σ_space²))` computed as f32
- Boundary: BORDER_REFLECT_101 via pre-padded image (no inline boundary checks)
- Rounding: Rust `f32::round()` (half-away-from-zero, matches OpenCV `cvRound` for non-half values)

### CLAHE — Max 1 Pixel Difference

**Reference:** `cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8,8)).apply(img)`

| Image | MAE | Max Error | Status |
|-------|-----|-----------|--------|
| gradient | 0.0938 | 1 | ±1 rounding |
| checker | 0.0000 | 0 | Exact |
| noisy_flat | 0.0262 | 1 | ±1 rounding |
| sharp_edges | 0.0081 | 1 | ±1 rounding |
| photo | 0.0253 | 1 | ±1 rounding |
| flat | 0.0000 | 0 | Exact |
| highcontrast | 0.0068 | 1 | ±1 rounding |

**Where it differs:** ~2-9% of pixels differ by exactly 1 intensity level on
images with smooth gradients. Checkerboard and flat images are pixel-exact.

**Why:** The bilinear interpolation between tile LUTs involves four f32
multiplications and additions. Rust and C++ f32 operations can round the
last mantissa bit differently on the same input, producing ±1 after the
final `round()` to u8. This is inherent to IEEE 754 f32 cross-platform
behavior and cannot be eliminated without using f64 (which would not match
OpenCV either).

**Alignment details (required for ±1 match):**
- LUT formula: `lut[i] = round(cumulative_sum × (255.0 / tile_pixels))` — OpenCV's `lutScale` convention, NOT the textbook `(cdf - cdf_min) × 255 / (N - cdf_min)` formula
- Clip computation: `clip = max(1, int(clipLimit × tilePixels / 256))` — integer truncation
- Residual redistribution: `step = max(histSize / residual, 1)`, stepping evenly across bins — NOT sequential first-N
- No identity shortcut: single-value tiles are processed normally through clip/redistribute/CDF (OpenCV does not special-case them)
- Interpolation coordinates: `txf = x / tileWidth - 0.5` with `floor()`, clamped to `[0, grid-1]`
- Tile centers at `(tx + 0.5) × tileWidth`; bilinear blend of 4 nearest tile LUTs

### Guided Filter — Max 1 Pixel Difference

**Reference:** `cv2.ximgproc.guidedFilter(img, img, radius=4, eps=0.01×255×255)`

| Image | MAE | Max Error | Status |
|-------|-----|-----------|--------|
| gradient | 0.0000 | 0 | Exact |
| checker | 0.0003 | 1 | ±1 rounding |
| noisy_flat | 0.0060 | 1 | ±1 rounding |
| sharp_edges | 0.0019 | 1 | ±1 rounding |
| photo | 0.0072 | 1 | ±1 rounding |
| flat | 0.0000 | 0 | Exact |
| highcontrast | 0.0024 | 1 | ±1 rounding |

**Where it differs:** <1% of pixels differ by exactly 1 on textured/noisy content.
Smooth gradient and flat images are pixel-exact.

**Why:** Same f32 rounding as CLAHE — the guided filter computes `mean_a × I + mean_b`
where `mean_a` and `mean_b` come from box means (integral image sums). The f32
summed-area-table accumulation rounds differently between Rust and C++ at the
last mantissa bit, producing ±1 after the final `round()` to u8.

**Alignment details (required for ±1 match):**
- Border mode: BORDER_REFLECT (edge pixel duplicated) — NOT BORDER_REFLECT_101
- Box mean: f32 summed area table with padded border, fixed `ksize × ksize` window
- Epsilon: in `[0,1]` normalized space; OpenCV uses pixel² units (`eps × 255²`)
- Algorithm: He et al. 2010 — local linear model `a×I + b`, averaged over window
- Self-guided: input used as both source and guidance image

## Reproducing Reference Fixtures

```bash
python3 -m venv /tmp/cv_venv
source /tmp/cv_venv/bin/activate
pip install numpy opencv-contrib-python-headless
python3 tests/fixtures/opencv/generate.py  # (generation script)
```

### Convolution Filters — Pixel-Exact

**Test file:** `tests/filter_reference_parity.rs`
**Venv:** `tests/fixtures/.venv` (Python 3.14, numpy 2.4.3, OpenCV 4.13.0, Pillow 12.1.1)

These tests run inline Python scripts via the venv to generate reference output,
then compare byte-for-byte against our Rust implementation. **No tests skip** —
if the venv is missing, tests fail with setup instructions.

#### Formula Operations — Pixel-Exact (MAE = 0.0000)

| Operation | Reference | Formula | Status |
|-----------|-----------|---------|--------|
| premultiply | Python `(c*a+127)//255` | Integer alpha multiply | Exact |
| add_alpha + remove_alpha | Self roundtrip | Byte insertion/removal | Exact |
| convolve (identity kernel) | Self | `[0,0,0,0,1,0,0,0,0]` | Exact |
| brightness(0) | Self | Identity | Exact |
| median (r=1) | Pillow `MedianFilter(3)` | Sorting-based median | Exact |

#### Color Operations — Pixel-Exact (MAE = 0.0000)

| Operation | Reference | Formula | Status |
|-----------|-----------|---------|--------|
| sepia | numpy matrix multiply | Microsoft `[[.393,.769,.189],[.349,.686,.168],[.272,.534,.131]]` | Exact |
| blend Multiply | numpy W3C CSS L1 | `(fg * bg + 127) // 255` | Exact |
| blend Screen | numpy W3C CSS L1 | `fg + bg - (fg * bg + 127) // 255` | Exact |

#### Spatial Operations — Pixel-Exact (MAE = 0.0000)

| Operation | Reference | Config | Status |
|-----------|-----------|--------|--------|
| convolve (sharpen 3x3) | OpenCV `filter2D` | `[[0,-1,0],[-1,5,-1],[0,-1,0]]`, `BORDER_REFLECT_101` | Exact |
| convolve (box blur 3x3) | OpenCV `blur` | `(3,3)`, `BORDER_REFLECT_101` | Exact |
| sobel | OpenCV `Sobel` | `CV_32F`, L2 magnitude, `BORDER_REFLECT_101` | Exact |

**Alignment details (convolution):**
- Border handling: reflect-edge padding matching OpenCV's `BORDER_REFLECT_101`
- Accumulation: f32 per channel, clamped to [0, 255]
- Rounding: truncation to u8 (matching OpenCV's saturate_cast)
- Separable detection: rank-1 kernels auto-detected and processed as two 1D passes

## Reference Tool Versions

All reference validation uses pinned tool versions. Changes to reference
versions require re-validation of all parity results.

### Python Venv (tests/fixtures/.venv)

| Package | Version | Used For |
|---------|---------|----------|
| Python | 3.14.3 | Script execution |
| numpy | 2.4.3 | Formula operations, array math |
| OpenCV | 4.13.0 | Spatial filters, bilateral, CLAHE, guided |
| Pillow | 12.1.1 | Median filter, format I/O |

Setup:
```bash
python3 -m venv tests/fixtures/.venv
tests/fixtures/.venv/bin/pip install numpy Pillow opencv-python-headless
```

### System Tools

| Tool | Version | Used For |
|------|---------|----------|
| ImageMagick | 7.1.2-18 Q16-HDRI | Fixture generation, encode/decode parity |
| libwebp (cwebp/dwebp) | 1.6.0 | VP8 encode/decode validation |
| libjpeg-turbo (cjpeg/djpeg) | 3.1.3 | JPEG encode/decode validation |
| libvips | 8.18.1 | Performance benchmarking reference |

## Match Tiers

| Tier | Requirement | When to Use |
|------|-------------|-------------|
| **EXACT** | MAE = 0.0, byte-identical | Formula ops, sorting-based filters, integer arithmetic |
| **DETERMINISTIC** | MAE < 0.1, max_err = 1 | f32 rounding differences (CLAHE, guided filter) |
| **ALGORITHM** | MAE < 2.0 | Different border modes, different intermediate precision |
| **DESIGN** | Structural similarity | Different algorithms for same goal (e.g., Sobel vs Laplacian) |

All new operations must target EXACT tier. DETERMINISTIC is acceptable only
when f32 cross-platform rounding is unavoidable (document why).

## Adding New Filters

When adding a new filter to `rasmcore-image`:

1. Implement the filter in `domain/filters.rs`
2. Add a parity test in `tests/filter_reference_parity.rs` using the venv Python
3. Generate reference output from the authoritative source (OpenCV preferred, numpy for formulas)
4. Verify MAE = 0.0000 (EXACT tier) or document why DETERMINISTIC/ALGORITHM tier is needed
5. Document the reference, formula, and alignment details in this file
6. Add the reference tool version to the version table above
