[package]
name = "rasmcore-trellis"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Codec-agnostic trellis quantization engine (Viterbi DP)"

[dependencies]
# No external dependencies — pure Rust, WASM-compatible

[dev-dependencies]
