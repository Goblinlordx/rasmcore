[package]
name = "rasmcore-dct"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Shared integer DCT/DST transforms (4x4 through 32x32) for JPEG, WebP, and HEVC codecs"

[dependencies]
# Zero external dependencies — pure Rust, WASM-ready

[dev-dependencies]
