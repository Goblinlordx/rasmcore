[package]
name = "rasmcore-bmp"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Pure Rust BMP encoder/decoder — palette, RLE4/RLE8, bitfields"

[dependencies]
rasmcore-bitio = { path = "../rasmcore-bitio" }

[dev-dependencies]
