id: lut-encoder-decoder_20260402030023Z
title: .cube LUT encoder + decoder — export/import color op chains as 3D LUT files
type: feature
status: pending
created: '2026-04-02'
updated: '2026-04-02'
spec:
  summary: 'Add .cube file encoder and decoder. The encoder short-circuits the pipeline:

    instead of executing the graph and encoding pixels, it runs fuse_color_ops(),

    extracts the fused ColorLut3D, and serializes to .cube format. The decoder

    parses a .cube file and injects a FusedClutNode into the graph. No pixels

    are involved in either direction.

    '
  context: "DaVinci Resolve can \"Generate LUT\" from a color grading node chain. No\nopen-source image library offers this.\
    \ rasmcore already has all the building\nblocks:\n- fuse_color_ops() walks the graph and composes consecutive color_op\
    \ nodes\n  into a single FusedClutNode (graph.rs:1127-1173)\n- ColorLut3D with compose_cluts(), from_fn(), lookup() (color_lut.rs)\n\
    - parse_cube_lut() reads .cube files (color_lut.rs:262-355)\n- 13 filters registered with color_op=\"true\" that produce\
    \ CLUTs\n- as_color_lut_op() trait method on ImageNode\n\nThe missing piece is trivial: serialize ColorLut3D back to .cube\
    \ text,\nand wire the encoder/decoder to short-circuit the pixel pipeline.\n\nThis gives rasmcore a unique capability\
    \ among image processing libraries:\nprogrammatic LUT generation from composable color operations.\n"
  codebase_analysis: "ENCODER SIDE:\n- sink.rs write_tiled() calls fuse_color_ops() at line 226 before encoding\n- After fusion,\
    \ color_op chains become FusedClutNode with as_color_lut_op()\n- The encoder needs a new branch: if format==\"cube\",\
    \ skip pixel execution,\n  extract the fused CLUT, serialize to .cube text\n- .cube format is trivial text: header + RGB\
    \ triplets (one per line)\n- Existing parse_cube_lut shows the format (LUT_3D_SIZE N, then N^3 lines)\n- R varies fastest,\
    \ then G, then B — matches ColorLut3D internal order\n\nDECODER SIDE:\n- parse_cube_lut() already returns ColorLut3D (color_lut.rs:262-355)\n\
    - Need to register .cube as a decodable format\n- Decoder returns a FusedClutNode (or equivalent) that can be composed\n\
    \  with further color ops in the pipeline\n- No pixels produced — the CLUT IS the decoded data\n\nPIPELINE BEHAVIOR:\n\
    - read(\"input.cube\") → injects FusedClutNode as source\n- color_op nodes chain on top → compose via fuse_color_ops()\n\
    - write(\"output.cube\") → extract fused CLUT → serialize\n- write(\"output.png\") from a .cube source → ERROR or apply\
    \ to identity image\n- read(\"photo.jpg\") + color ops + write(\"output.cube\") → ignore image source,\n  fuse color ops\
    \ only, serialize CLUT\n\nREGISTRATION:\n- Encoder: StaticEncoderRegistration (name=\"cube\", format=\"cube\",\n  mime=\"\
    application/cube-lut\", extensions=[\"cube\"])\n- Decoder: detect_format should recognize .cube by \"LUT_3D_SIZE\" header\
    \ text\n"
  acceptance_criteria:
  - write("output.cube") produces valid .cube file from color op chain
  - Round-trip test passes — parse .cube, re-export, parse again, CLUTs match
  - Composed color ops (hue_rotate + asc_cdl + saturate) produce correct fused .cube
  - read("input.cube") correctly injects CLUT into pipeline
  - read("input.cube") + more color ops + write("output.cube") composes correctly
  - Grid size defaults to 33 (industry standard), configurable
  - .cube output matches parse_cube_lut input format exactly
  - No pixel execution occurs for .cube → .cube pipeline
  - cargo build + cargo component build succeed
  out_of_scope: 'Hald CLUT image export (future — trivial given ColorLut3D + PNG encoder).

    1D LUT export (3D captures everything including cross-channel ops).

    Other LUT formats (.csp, .3dl, .spi3d) — can add later as separate encoders.

    LUT visualization or preview. LUT optimization/reduction.

    '
  technical_notes: "ENCODER IMPLEMENTATION:\n1. Add encoder/cube.rs with:\n   - CubeEncodeConfig { grid_size: usize, title:\
    \ Option<String> }\n   - fn serialize_cube(lut: &ColorLut3D, config: &CubeEncodeConfig) -> String\n   - Format: \"LUT_3D_SIZE\
    \ {N}\\n\" then N^3 lines of \"R G B\\n\" (6 decimal places)\n   - inventory::submit! registration\n2. Modify sink.rs\
    \ write path:\n   - After fuse_color_ops(), if format == \"cube\":\n     - Walk graph to find the terminal FusedClutNode\
    \ (or compose all color_op nodes)\n     - If no color ops found, export identity CLUT\n     - Call serialize_cube() and\
    \ return bytes\n     - Skip pixel execution entirely\n\nDECODER IMPLEMENTATION:\n1. Add decoder support for .cube format\
    \ detection:\n   - detect_format: check for \"LUT_3D_SIZE\" in first 512 bytes of text\n2. When .cube is decoded:\n  \
    \ - Call existing parse_cube_lut()\n   - Return a special decoded result that carries a ColorLut3D\n   - Pipeline source\
    \ node wraps this as a FusedClutNode\n3. Pipeline behavior:\n   - If source is CLUT and sink is .cube: pure CLUT composition,\
    \ no pixels\n   - If source is CLUT and sink is image format: apply CLUT to identity\n     gradient or error (TBD)\n\n\
    TESTING:\n- Unit test: serialize identity CLUT → parse back → exact match\n- Unit test: serialize composed ops → parse\
    \ back → apply to test pixels →\n  compare vs applying ops directly\n- Round-trip: .cube file → parse → serialize → byte-equal\n\
    - Integration: pipeline with color ops → write .cube → read .cube →\n  apply to image → compare vs original pipeline\n"
plan:
- phase: Encoder
  tasks:
  - text: Add encoder/cube.rs with serialize_cube(lut, config) -> String
    done: true
  - text: Add CubeEncodeConfig with grid_size (default 33) and optional title
    done: true
  - text: Register .cube encoder via inventory::submit!
    done: true
  - text: Modify sink write path to short-circuit for format=="cube" — extract fused CLUT, skip pixel execution
    done: true
  - text: Unit test — identity CLUT serializes to valid .cube, round-trips through parse_cube_lut
    done: true
  - text: Unit test — composed color ops (hue_rotate + saturate) produce correct .cube output
    done: true
- phase: Decoder
  tasks:
  - text: Add .cube format detection in detect_format (check for LUT_3D_SIZE header)
    done: true
  - text: Add decoder path that returns ColorLut3D from .cube file via parse_cube_lut
    done: true
  - text: Wire decoded CLUT as FusedClutNode source in pipeline graph
    done: true
  - text: Unit test — read .cube, chain color ops, write .cube produces correct composed output
    done: true
- phase: Integration and verification
  tasks:
  - text: Integration test — full pipeline with mixed ops, write .cube only captures color ops
    done: true
  - text: Parity test — apply exported .cube to image vs applying ops directly, pixel-exact match
    done: true
  - text: cargo build + cargo component build succeed
    done: true
  - text: Verify all existing tests pass (no regressions)
    done: true
extra: {}
