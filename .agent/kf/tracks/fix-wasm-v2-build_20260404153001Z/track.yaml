id: fix-wasm-v2-build_20260404153001Z
title: Fix V2 WASM component build — WIT/adapter sync
type: bug
status: pending
created: '2026-04-04'
updated: '2026-04-03'
spec:
  summary: 'Fix 19 cargo-component build errors from V2 migration. The native build

    passes but WASM fails due to WIT/adapter desync.

    '
  context: 'The V2 pipeline migration changed ConfigParams structs (derive(Filter)

    pattern), added CacheQuality enum, and modified function signatures.

    The WIT template and adapter code weren''t fully updated to match.

    '
  codebase_analysis: "19 errors on cargo component build -p rasmcore-image:\n- 12 type mismatches in generated_pipeline_adapter.rs\
    \ — V2 derive(Filter)\n  changed field types (color structs vs tuples) but WIT config records\n  weren't regenerated\n\
    - 4 CacheQuality not found — cache-quantize-opt track added enum and\n  set_cache_quality method but WIT template lacks\
    \ the enum/method\n- 2 argument count mismatches — function signatures changed during V2\n- 1 method not member of trait\
    \ — set_cache_quality not in WIT\n"
  acceptance_criteria:
  - cargo component build -p rasmcore-image succeeds
  - WIT template includes CacheQuality enum and set-cache-quality method
  - All V2 derive(Filter) config records match WIT binding types
  - Native cargo check still passes
  out_of_scope: 'Test compilation errors (separate issue from filter-migrate-remaining).

    New feature work.

    '
  technical_notes: "Fix approach:\n1. Add cache-quality enum + set-cache-quality to pipeline.wit.tmpl\n2. Regenerate pipeline.wit\
    \ from template (touch template, rebuild)\n3. Fix type mismatches — likely color param records need updating\n   in the\
    \ codegen or the WIT config record generation\n4. Fix argument count mismatches in adapter methods\n"
plan:
- phase: WIT template fixes
  tasks:
  - text: Add cache-quality enum and set-cache-quality method to pipeline.wit.tmpl
    done: true
  - text: Regenerate pipeline.wit and verify bindings match
    done: true
- phase: Adapter type fixes
  tasks:
  - text: Fix 12 type mismatches in generated pipeline adapter (color struct fields)
    done: true
  - text: Fix 2 argument count mismatches
    done: true
- phase: Verification
  tasks:
  - text: cargo component build -p rasmcore-image succeeds with 0 errors
    done: true
extra: {}
