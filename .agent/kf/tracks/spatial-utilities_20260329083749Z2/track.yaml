id: spatial-utilities_20260329083749Z2
title: Spatial Utilities — Connected Components, Flood Fill, Image Pyramids
type: feature
status: pending
created: '2026-03-29'
updated: '2026-03-29'
spec:
  summary: Connected component labeling, flood fill (4/8-connectivity), and Gaussian/Laplacian image pyramid construction.
    All testable against OpenCV 4.13.
  context: ''
  codebase_analysis: ''
  acceptance_criteria:
  - All operations validated against reference implementation (OpenCV/colour-science/GIMP)
  - Reference parity tests added to opencv_parity.rs with fixtures
  - SIMD optimization where applicable (per project feedback)
  - Registered in FilterRegistry with param descriptors
  - Validated against canonical 7-image test set (gradient, checker, noisy_flat, sharp_edges, photo, flat, highcontrast) where
    applicable
  - Reference fixtures generated from OpenCV/colour-science/GIMP in venv and stored in tests/fixtures/opencv/
  - Parity tier documented in REFERENCE.md (EXACT, F64_LIMIT, F32_ROUNDING, or REF_PRECISION)
  out_of_scope: GPU acceleration. Neural network approaches. SIMD optimization deferred to separate track if needed.
  technical_notes: 'Connected components: two-pass algorithm with union-find. Flood fill: BFS/DFS from seed with tolerance.
    Pyramids: Gaussian pyrDown = blur+subsample, pyrUp = upsample+blur. Laplacian = level - pyrUp(pyrDown(level)).'
plan:
- phase: Implementation + Validation
  tasks:
  - text: Implement connected component labeling (4-connectivity and 8-connectivity)
    done: true
  - text: Implement flood fill with configurable tolerance and connectivity
    done: true
  - text: Implement Gaussian pyramid (pyrDown/pyrUp)
    done: true
  - text: Implement Laplacian pyramid construction and reconstruction
    done: true
  - text: Generate OpenCV reference fixtures and parity tests
    done: true
extra: {}
