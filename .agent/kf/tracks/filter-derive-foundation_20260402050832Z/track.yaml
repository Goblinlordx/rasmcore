id: filter-derive-foundation_20260402050832Z
title: 'Filter derive foundation — #[derive(Filter)] + CpuFilter/GpuFilter/PointOp/ColorOp traits'
type: feature
status: pending
created: '2026-04-02'
updated: '2026-04-02'
spec:
  summary: 'Implement the new unified filter registration architecture: one derive macro

    (#[derive(Filter)]) that replaces both #[register_filter] and #[derive(ConfigParams)].

    Define CpuFilter, GpuFilter, PointOp, ColorOp traits. Codegen supports BOTH

    old (bare fn) and new (struct + trait) patterns during migration. No existing

    filters are migrated in this track — only the infrastructure.

    '
  context: "Current architecture per filter (4 separate pieces):\n1. BlurParams — #[derive(ConfigParams)] config struct\n\
    2. blur() — bare fn with #[register_filter], CPU logic\n3. BlurNode — codegen-generated pipeline node, wraps params +\
    \ fn\n4. impl GpuCapable for BlurNode — hand-written in gpu_impls.rs\n\nTarget architecture per filter (1 piece):\n1.\
    \ Blur — #[derive(Filter)] struct with #[filter(...)] + #[param(...)]\n   impl CpuFilter for Blur — CPU compute logic\
    \ (required)\n   impl GpuFilter for Blur — GPU shader dispatch (optional)\n   impl PointOp for Blur — 1D LUT fusion (optional)\n\
    \   impl ColorOp for Blur — 3D CLUT fusion (optional)\n\nThe derive generates: ConfigParams (param_descriptors, Default),\
    \ inventory\nregistration, pipeline node wrapper. Codegen detects which optional traits\nare implemented and generates\
    \ appropriate dispatch.\n\nBOTH patterns must coexist during migration. Old bare-fn filters and new\nderive-based filters\
    \ must work side by side in the same build.\n"
  codebase_analysis: "FILES TO CREATE/MODIFY:\n- rasmcore-macros/src/lib.rs — add #[derive(Filter)] proc macro\n- rasmcore-pipeline/src/traits.rs\
    \ (new) — CpuFilter, GpuFilter, PointOp, ColorOp trait definitions\n- rasmcore-codegen/src/parse/filters.rs — detect new-style\
    \ struct registrations alongside old bare-fn\n- rasmcore-codegen/src/generate/pipeline.rs — generate nodes from new-style\
    \ registrations\n- rasmcore-codegen/src/generate/cli_dispatch.rs — support both old and new dispatch\n- rasmcore-image/build.rs\
    \ — parse both styles\n\nTRAIT SIGNATURES:\nCpuFilter:\n  fn compute(&self, request: Rect, upstream: &mut UpstreamFn,\
    \ info: &ImageInfo) -> Result<Vec<u8>>\nGpuFilter:\n  fn gpu_ops(&self, width: u32, height: u32) -> Option<Vec<GpuOp>>\n\
    PointOp:\n  fn build_lut(&self) -> [u8; 256]\nColorOp:\n  fn build_clut(&self) -> ColorLut3D\n\nCOEXISTENCE STRATEGY:\n\
    - Old filters use #[register_filter] on bare fn + separate ConfigParams\n- New filters use #[derive(Filter)] on struct\
    \ + trait impls\n- Codegen detects both and generates pipeline nodes for both\n- param-manifest.json includes both\n-\
    \ Migration happens filter-by-filter in subsequent tracks\n"
  acceptance_criteria:
  - derive(Filter) macro generates ConfigParams + registration + pipeline node
  - CpuFilter trait defined and used by codegen for compute dispatch
  - GpuFilter trait defined and detected by codegen for GPU dispatch
  - PointOp trait defined and detected by codegen for LUT fusion
  - ColorOp trait defined and detected by codegen for CLUT fusion
  - At least 1 example filter migrated to prove the pattern works
  - All existing 140+ bare-fn filters continue to work unchanged
  - Both old and new styles coexist in the same build
  - cargo build + cargo component build succeed
  - Full test suite passes
  out_of_scope: 'Migrating all filters (separate tracks). Changing filter behavior.

    Removing old registration infrastructure (kept for coexistence).

    '
  technical_notes: "DERIVE MACRO DESIGN:\n\nInput:\n#[derive(Filter)]\n#[filter(name = \"blur\", category = \"spatial\")]\n\
    struct Blur {\n    #[param(min = 0.0, max = 100.0, default = 3.0)]\n    pub radius: f32,\n}\n\nGenerated output:\n- impl\
    \ Blur { fn param_descriptors() -> Vec<ParamDescriptorJson> { ... } }\n- impl Default for Blur { ... }\n- static __RASMCORE_FILTER_BLUR:\
    \ StaticFilterRegistration = ...;\n- inventory::submit!(&__RASMCORE_FILTER_BLUR);\n\nCODEGEN DETECTION:\nBuild.rs parses\
    \ both:\n- Old: #[register_filter] on fn → FilterReg with fn_name\n- New: #[derive(Filter)] on struct → FilterReg with\
    \ struct_name + trait detection\n\nFor new-style filters, codegen scans for impl blocks:\n- impl CpuFilter for Blur →\
    \ generates compute_region delegation\n- impl GpuFilter for Blur → generates GpuCapable delegation\n- impl PointOp for\
    \ Blur → generates as_point_op_lut override\n- impl ColorOp for Blur → generates as_color_lut_op override\n\nPIPELINE\
    \ NODE GENERATION (new style):\npub struct BlurNode {\n    config: Blur,\n    upstream: u32,\n    source_info: ImageInfo,\n\
    }\nimpl ImageNode for BlurNode {\n    fn compute_region(&self, request, upstream_fn) -> Result<Vec<u8>> {\n        self.config.compute(request,\
    \ &mut |r| upstream_fn(self.upstream, r), &self.source_info)\n    }\n    fn as_point_op_lut(&self) -> Option<[u8; 256]>\
    \ {\n        Some(self.config.build_lut())  // only if PointOp is impl'd\n    }\n}\nimpl GpuCapable for BlurNode {  //\
    \ only if GpuFilter is impl'd\n    fn gpu_ops(&self, w, h) -> Option<Vec<GpuOp>> {\n        self.config.gpu_ops(w, h)\n\
    \    }\n}\n\nMIGRATION SCRIPT PER FILTER:\n1. validate (old form tests pass)\n2. deregister (remove #[register_filter]\
    \ from bare fn)\n3. validate removal (build ok, filter absent from manifest)\n4. implement new form (#[derive(Filter)]\
    \ + CpuFilter impl)\n5. validate new (build ok, filter in manifest, all tests pass)\nEach step is a valid commit.\n"
plan:
- phase: Trait definitions
  tasks:
  - text: Define CpuFilter trait in rasmcore-pipeline (or rasmcore-image domain)
    done: true
  - text: Define GpuFilter trait (reusing GpuOp from rasmcore-pipeline)
    done: true
  - text: Define PointOp trait (replaces LutPointOp)
    done: true
  - text: Define ColorOp trait (replaces ColorLutOp)
    done: true
- phase: Derive macro
  tasks:
  - text: Implement derive(Filter) proc macro — generates ConfigParams + registration
    done: true
  - text: Support
    done: true
  - text: Support
    done: true
  - text: Generate inventory::submit! from derive
    done: true
- phase: Codegen support
  tasks:
  - text: Update codegen parser to detect derive(Filter) struct registrations
    done: true
  - text: Update pipeline node generator to emit nodes from struct-based filters
    done: true
  - text: Detect CpuFilter/GpuFilter/PointOp/ColorOp impls and generate appropriate node methods
    done: true
  - text: Update CLI dispatch to handle both old and new filter styles
    done: true
- phase: Proof of concept + verification
  tasks:
  - text: Migrate 1 simple filter (e.g., invert or gamma) to prove the pattern
    done: true
  - text: Migrate 1 GPU filter (e.g., spherize) to prove GPU trait integration
    done: true
  - text: Verify old filters still work alongside new ones
    done: true
  - text: Full test suite passes, WASM build clean
    done: true
extra: {}
