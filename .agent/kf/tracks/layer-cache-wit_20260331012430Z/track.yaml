id: layer-cache-wit_20260331012430Z
title: LayerCache WIT Resource & Pipeline Adapter Wiring
type: feature
status: pending
created: '2026-03-31'
updated: '2026-03-31'
spec:
  summary: 'Expose LayerCache as a WIT resource and wire it into the pipeline

    adapter so WASM consumers can create, share, and manage layer caches

    across pipeline invocations.

    '
  context: 'The LayerCache domain implementation is complete (rasmcore-pipeline

    layer_cache.rs + NodeGraph integration in graph.rs). It works from

    native Rust but is not yet accessible from WASM consumers via WIT.

    This track adds the WIT resource definition, adapter wiring, and

    ensures the pipeline constructor accepts an optional cache handle.

    '
  codebase_analysis: 'EXISTING:

    - LayerCache struct in rasmcore-pipeline/src/layer_cache.rs

    - NodeGraph::with_layer_cache() constructor

    - NodeGraph::finalize_layer_cache() for post-write cleanup

    - PipelineResource in adapter/pipeline_adapter.rs creates NodeGraph


    NEEDS:

    - WIT resource definition for layer-cache in pipeline.wit

    - Adapter struct wrapping Rc<RefCell<LayerCache>>

    - Pipeline constructor extended to accept optional borrow<layer-cache>

    - Pipeline write methods call finalize_layer_cache() after encoding

    - Pipeline node-add methods compute content hashes and use add_node_with_hash


    PATTERN TO FOLLOW:

    - ImagePipeline is already a WIT resource (pipeline_adapter.rs)

    - It uses RefCell<NodeGraph> internally

    - Adding a second resource (LayerCache) follows the same pattern

    '
  acceptance_criteria:
  - layer-cache WIT resource with constructor(memory-budget-mb), stats(), clear()
  - Pipeline constructor accepts optional borrow<layer-cache>
  - Pipeline write methods call finalize_layer_cache() on completion
  - Pipeline node-add methods compute and pass content hashes
  - cache-stats WIT record with entries, hits, misses, size-bytes
  - WASM component builds successfully with new WIT types
  - Test from TypeScript SDK showing cache reuse across two pipeline calls
  out_of_scope: 'Domain layer changes (already complete). Additional cache strategies.

    Disk persistence. Cache serialization.

    '
  technical_notes: "WIT ADDITIONS (pipeline.wit):\n\nresource layer-cache {\n    constructor(memory-budget-mb: u32);\n   \
    \ stats: func() -> cache-stats;\n    clear: func();\n}\n\nrecord cache-stats {\n    entries: u32,\n    hits: u64,\n  \
    \  misses: u64,\n    size-bytes: u64,\n}\n\nExtend image-pipeline resource:\n- Add constructor variant or init method\
    \ accepting layer-cache\n\nADAPTER WIRING:\n- LayerCacheResource wraps Rc<RefCell<LayerCache>>\n- PipelineResource stores\
    \ Option<Rc<RefCell<LayerCache>>>\n- On read(): compute source hash, use add_node_with_hash\n- On filter/transform: compute\
    \ chain hash, use add_node_with_hash\n- On write_*(): call graph.finalize_layer_cache() after encoding\n\nHASH COMPUTATION\
    \ IN ADAPTER:\nEach node-add method needs to:\n1. Get upstream hash from graph.node_hash(upstream_id)\n2. Compute this_hash\
    \ = compute_hash(upstream, op_name, param_bytes)\n3. Call graph.add_node_with_hash(node, this_hash)\n"
plan:
- phase: WIT Definition
  tasks:
  - text: Add layer-cache resource and cache-stats record to pipeline.wit
    done: true
  - text: Add set-layer-cache method to image-pipeline resource
    done: true
- phase: Adapter Implementation
  tasks:
  - text: Implement LayerCacheResource adapter struct
    done: true
  - text: Wire pipeline constructor to accept and store layer cache
    done: true
  - text: Add hash computation to pipeline read/filter/transform methods
    done: true
  - text: Call finalize_layer_cache in all write methods
    done: true
- phase: Verification
  tasks:
  - text: cargo component check passes with new WIT types
    done: true
  - text: Existing pipeline tests pass unchanged
    done: true
  - text: Test layer cache reuse across two pipeline invocations
    done: true
extra: {}
