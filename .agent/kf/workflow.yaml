# Workflow — rasmcore
version: "1.0"

verification:
  command: "./scripts/validate.sh"
  description: "Format check (rustfmt), lint (clippy), domain tests, parity tests, WASM build"

phases:
  - name: implementation
    description: "Full implementation of core modules"
    current: true
  - name: integration
    description: "Host SDK development and cross-language testing"
